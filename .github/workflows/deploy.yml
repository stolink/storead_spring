name: Deploy

on:
  push:
    branches:
      - release # production 환경
      - dev # develop 환경

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  id-token: write
  packages: write
  contents: read

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          java-version: "21"
          distribution: "temurin"
          cache: gradle

      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v4

      - name: Grant execute permission for gradlew
        run: chmod +x gradlew

      - name: Run tests
        run: ./gradlew test

  build-and-push:
    runs-on: ubuntu-24.04-arm # ARM64 native runner (에뮬레이션 오버헤드 제거)
    # 브랜치에 따라 환경 자동 선택
    environment: ${{ github.ref == 'refs/heads/release' && 'production' || 'develop' }}
    outputs:
      image-tag: ${{ steps.meta.outputs.tags }}
      account-id: ${{ steps.aws.outputs.account_id }}
      sha-short: ${{ steps.vars.outputs.sha_short }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          java-version: "21"
          distribution: "temurin"
          cache: gradle

      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v4

      - name: Grant execute permission for gradlew
        run: chmod +x gradlew

      - name: Build with Gradle
        run: ./gradlew build -x test

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Configure AWS credentials (OIDC)
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: ${{ secrets.AWS_ROLE_SSMFULL_ARN }}
          aws-region: ${{ vars.AWS_REGION }}

      - name: Get AWS Account ID
        id: aws
        run: echo "account_id=$(aws sts get-caller-identity --query Account --output text)" >> $GITHUB_OUTPUT

      - name: Set Short SHA
        id: vars
        run: echo "sha_short=$(echo ${{ github.sha }} | cut -c1-7)" >> $GITHUB_OUTPUT

      - name: Login to ECR
        uses: aws-actions/amazon-ecr-login@v2

      - name: Extract metadata for Docker
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ steps.aws.outputs.account_id }}.dkr.ecr.${{ vars.AWS_REGION }}.amazonaws.com/stolink/storead-spring
          tags: |
            type=raw,value=${{ github.ref == 'refs/heads/release' && 'latest' || 'dev-latest' }}
            type=sha,prefix=${{ github.ref == 'refs/heads/release' && 'sha-' || 'dev-sha-' }}

      - name: Build and push
        uses: docker/build-push-action@v5
        with:
          context: .
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

  deploy:
    needs: [test, build-and-push]
    runs-on: ubuntu-latest
    # 브랜치에 따라 환경 자동 선택
    environment: ${{ github.ref == 'refs/heads/release' && 'production' || 'develop' }}
    steps:
      - name: Checkout (for docker-compose.yml only)
        uses: actions/checkout@v4
        with:
          sparse-checkout: |
            docker-compose.yml
          sparse-checkout-cone-mode: false

      - name: Configure AWS credentials (OIDC)
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: ${{ secrets.AWS_ROLE_SSMFULL_ARN }}
          aws-region: ${{ vars.AWS_REGION }}

      - name: Deploy to EC2 via SSM
        env:
          POSTGRESQL_URL: ${{ secrets.POSTGRESQL_URL }}
          POSTGRESQL_USERNAME: ${{ secrets.POSTGRESQL_USERNAME }}
          POSTGRESQL_PASSWORD: ${{ secrets.POSTGRESQL_PASSWORD }}
          POSTGRESQL_PORT: ${{ vars.POSTGRESQL_PORT }}
          NEO4J_URI: ${{ secrets.NEO4J_URI }}
          NEO4J_USERNAME: ${{ secrets.NEO4J_USERNAME }}
          NEO4J_PASSWORD: ${{ secrets.NEO4J_PASSWORD }}
          NEO4J_PORT: ${{ vars.NEO4J_PORT }}
          CLOUDFRONT_URL: ${{ vars.CLOUDFRONT_URL }}
          STOREAD_CLOUDFRONT_URL: ${{ vars.STOREAD_CLOUDFRONT_URL }}
          GOOGLE_CLIENT_ID: ${{ secrets.GOOGLE_CLIENT_ID }}
          GOOGLE_CLIENT_SECRET: ${{ secrets.GOOGLE_CLIENT_SECRET }}
          JWT_SECRET: ${{ secrets.JWT_SECRET }}
          # ECR 이미지 정보
          AWS_ACCOUNT_ID: ${{ needs.build-and-push.outputs.account-id }}
          # 브랜치에 따라 프로파일 및 이미지 태그 결정 (SSM 배포용)
          SPRING_PROFILE: ${{ github.ref == 'refs/heads/release' && 'prod' || 'dev' }}
          IMAGE_TAG: ${{ github.ref == 'refs/heads/release' && 'latest' || 'dev-latest' }}
          # Parameter Store용 고유 SHA 태그 (ASG 신규 인스턴스용 - ECR 태그와 일치)
          UNIQUE_TAG: ${{ github.ref == 'refs/heads/release' && format('sha-{0}', needs.build-and-push.outputs.sha-short) || format('dev-sha-{0}', needs.build-and-push.outputs.sha-short) }}
          # 운영 환경에서는 localhost 제외 (보안 강화)
          CORS_ORIGINS: ${{ github.ref == 'refs/heads/release' && format('https://{0},https://{1}', vars.CLOUDFRONT_URL, vars.STOREAD_CLOUDFRONT_URL) || format('http://localhost:3000,http://localhost:5173,http://localhost:5174,https://{0},https://{1}', vars.CLOUDFRONT_URL, vars.STOREAD_CLOUDFRONT_URL) }}
        run: |
          # .env 파일 내용 생성
          ENV_CONTENT="POSTGRESQL_URL=${POSTGRESQL_URL}
          POSTGRESQL_USERNAME=${POSTGRESQL_USERNAME}
          POSTGRESQL_PASSWORD=${POSTGRESQL_PASSWORD}
          POSTGRESQL_PORT=${POSTGRESQL_PORT}
          NEO4J_URI=${NEO4J_URI}
          NEO4J_USERNAME=${NEO4J_USERNAME}
          NEO4J_PASSWORD=${NEO4J_PASSWORD}
          NEO4J_PORT=${NEO4J_PORT}
          IMAGE_NAME=${AWS_ACCOUNT_ID}.dkr.ecr.${{ vars.AWS_REGION }}.amazonaws.com/stolink/storead-spring:${UNIQUE_TAG}
          CORS_ALLOWED_ORIGINS=${CORS_ORIGINS}
          OAUTH2_REDIRECT_URI=https://${STOREAD_CLOUDFRONT_URL}/oauth2/callback
          OAUTH2_DEFAULT_FAILURE_URL=https://${STOREAD_CLOUDFRONT_URL}/oauth2/callback
          OAUTH2_ALLOWED_REDIRECT_HOSTS=${CLOUDFRONT_URL}
          GOOGLE_CLIENT_ID=${GOOGLE_CLIENT_ID}
          GOOGLE_CLIENT_SECRET=${GOOGLE_CLIENT_SECRET}
          JWT_SECRET=${JWT_SECRET}
          JWT_COOKIE_DOMAIN=${CLOUDFRONT_URL}
          SPRING_PROFILES_ACTIVE=${SPRING_PROFILE}"

          # docker-compose.yml 내용 읽기
          COMPOSE_CONTENT=$(cat docker-compose.yml)

          # Base64로 인코딩하여 SSM으로 전달 (특수문자 이스케이프 문제 해결)
          ENV_BASE64=$(echo "$ENV_CONTENT" | base64 -w 0)
          COMPOSE_BASE64=$(echo "$COMPOSE_CONTENT" | base64 -w 0)

          # Parameter Store에 저장 (ASG 신규 인스턴스 자동 설정용)
          ENV_PATH=${{ github.ref == 'refs/heads/release' && '/prod' || '/dev' }}/storead-spring
          aws ssm put-parameter --name "${ENV_PATH}/env" --value "$ENV_CONTENT" --type SecureString --overwrite
          aws ssm put-parameter --name "${ENV_PATH}/docker-compose" --value "$COMPOSE_CONTENT" --type String --overwrite
          echo "✅ Parameter Store updated: ${ENV_PATH}"

          # SSM 명령 실행 및 Command ID 캡처
          # dev 브랜치: dev-storead-spring 태그, release 브랜치: storead-spring 태그
          EC2_TAG=${{ github.ref == 'refs/heads/release' && 'storead-spring' || 'dev-storead-spring' }}

          COMMAND_ID=$(aws ssm send-command \
            --document-name "AWS-RunShellScript" \
            --targets "Key=tag:tag,Values=${EC2_TAG}" \
            --parameters "commands=[
              \"sudo -i -u ubuntu bash -c 'mkdir -p /home/ubuntu/storead-project'\",
              \"sudo rm -f /home/ubuntu/storead-project/.env && echo '${ENV_BASE64}' | base64 -d | sudo -u ubuntu tee /home/ubuntu/storead-project/.env > /dev/null\",
              \"sudo rm -f /home/ubuntu/storead-project/docker-compose.yml && echo '${COMPOSE_BASE64}' | base64 -d | sudo -u ubuntu tee /home/ubuntu/storead-project/docker-compose.yml > /dev/null\",
              \"sudo -i -u ubuntu bash -c 'aws ecr get-login-password --region ap-northeast-2 | sudo docker login --username AWS --password-stdin ${AWS_ACCOUNT_ID}.dkr.ecr.ap-northeast-2.amazonaws.com'\",
              \"sudo -i -u ubuntu bash -c 'cd /home/ubuntu/storead-project && sudo docker compose down --remove-orphans && sudo docker compose up -d --pull always && sudo docker image prune -f'\"
            ]" \
            --timeout-seconds 600 \
            --max-concurrency "50%" \
            --max-errors "0" \
            --query "Command.CommandId" \
            --output text)

          echo "SSM Command ID: $COMMAND_ID"

          # SSM 명령 완료 대기 (최대 10분)
          echo "Waiting for SSM command to complete..."
          for i in {1..60}; do
            STATUS=$(aws ssm list-commands --command-id "$COMMAND_ID" --query "Commands[0].Status" --output text)
            echo "Attempt $i/60: Status = $STATUS"

            if [ "$STATUS" = "Success" ]; then
              echo "✅ SSM command completed successfully"
              break
            elif [ "$STATUS" = "Failed" ] || [ "$STATUS" = "Cancelled" ] || [ "$STATUS" = "TimedOut" ]; then
              echo "❌ SSM command failed with status: $STATUS"
              # 실패 시 상세 로그 출력
              aws ssm list-command-invocations --command-id "$COMMAND_ID" --details --query "CommandInvocations[0].CommandPlugins[0].Output" --output text
              exit 1
            fi

            sleep 10
          done

          if [ "$STATUS" != "Success" ]; then
            echo "❌ SSM command did not complete in time. Last status: $STATUS"
            exit 1
          fi

      - name: Health Check
        run: |
          echo "Waiting for application to start..."
          sleep 60

          HEALTH_URL="https://${{ vars.STOREAD_CLOUDFRONT_URL }}/storead/actuator/health"
          echo "Health check URL: $HEALTH_URL"

          for i in {1..5}; do
            echo "Health check attempt $i/5..."
            HTTP_CODE=$(curl -s -o /tmp/health_response.txt -w "%{http_code}" --max-time 10 "$HEALTH_URL" 2>&1)
            
            if [ "$HTTP_CODE" = "200" ]; then
              echo "✅ Health check passed!"
              cat /tmp/health_response.txt
              exit 0
            fi
            
            echo "❌ Attempt $i failed with HTTP code: $HTTP_CODE"
            echo "Response body:"
            cat /tmp/health_response.txt 2>/dev/null || echo "(no response body)"
            echo ""
            echo "Retrying in 10 seconds..."
            sleep 10
          done

          echo "❌ Health check failed after 5 attempts"
          echo "Last HTTP code: $HTTP_CODE"
          exit 1
